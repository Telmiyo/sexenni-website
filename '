import Head from 'next/head'
import {Container} from '@chakra-ui/react'

const Layout = ({children, title}) => (
		<Head>
		<title>{title}</title>
		</Head>
		<Container pt={8}>
				{children}
		</Container>
)
export Layout
